node_modules/
dist/
coverage/
test-results/
playwright-report/
blob-report/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.enc
.DS_Store
Thumbs.db

.vercel
.release/
.env*

# Generated Vercel function bundle (scripts/build-api.mjs)
api/index.mjs

# Generated by scripts/build-api.mjs
/public/
apps/driver/public/tesseract/

# tesseract unpacks the gzipped models beside the process that ran it (scripts/ocr-calibrate.mjs);
# they are ~5 MB of build output, staged by scripts/copy-tesseract.mjs, never source.
*.traineddata

# Database backups. Real business data — password hashes, session tokens, the whole ledger.
# Never committed; kept off-machine instead (see RUNBOOK).
backups/

# Gemini OCR benchmark output — real customer addresses and GPS. Same rule as backups/.
# Kept OUTSIDE the repo (~/Desktop/ash-ocr-runs); this is belt and braces.
ash-ocr-runs/
tools/gemini-relay/public/

# Claude Code local settings (personal; holds deploy rights)
.claude/settings.local.json
