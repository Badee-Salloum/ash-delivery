{
  "name": "@ash/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "dependencies": {
    "@ash/adapters": "workspace:*",
    "@ash/contracts": "workspace:*",
    "@ash/db": "workspace:*",
    "@ash/domain": "workspace:*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.2.2",
    "bcryptjs": "^3.0.2",
    "fastify": "^5.2.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run",
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "migrate": "node src/migrate-cli.ts",
    "seed:demo": "node src/seed-cli.ts"
  }
}
