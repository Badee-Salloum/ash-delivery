{
  "name": "ash-delivery",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "tsc -p packages/domain/tsconfig.json && tsc -p packages/contracts/tsconfig.json && tsc -p packages/testkit/tsconfig.json && tsc -p packages/adapters/tsconfig.json && tsc -p packages/db/tsconfig.json && tsc -p packages/client/tsconfig.json && tsc -p apps/api/tsconfig.json",
    "check:domain-pure": "node scripts/check-domain-pure.mjs",
    "check:sql": "node scripts/check-sql.mjs",
    "check:wire": "node scripts/check-wire-money.mjs",
    "check": "pnpm typecheck && pnpm check:domain-pure && pnpm check:sql && pnpm check:wire && pnpm check:strip && pnpm test",
    "dev:db": "docker compose -f infra/compose/docker-compose.dev.yml up -d",
    "check:strip": "node scripts/check-strippable.mjs",
    "start": "node apps/api/src/server.ts",
    "migrate": "pnpm --filter @ash/api migrate",
    "seed:demo": "pnpm --filter @ash/api seed:demo"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.2"
  }
}
