{
  "name": "@ash/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "comment": "Wire schemas + the ports that keep the domain unaware of I/O. Zod only.",
  "dependencies": {
    "@ash/domain": "workspace:*",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests"
  }
}
