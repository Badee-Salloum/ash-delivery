{
  "name": "@ash/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "comment": "PURE. `dependencies` MUST stay empty — asserted by scripts/check-domain-pure.mjs in CI.",
  "dependencies": {},
  "devDependencies": {
    "fast-check": "^4.1.1",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run"
  }
}
