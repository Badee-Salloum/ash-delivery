{
  "name": "@ash/testkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./conformance": "./src/conformance.ts"
  },
  "comment": "One conformance suite, run against every implementation of the ports. A behaviour that differs between memory and Postgres is a bug in one of them, and this is how it gets found.",
  "dependencies": {
    "@ash/contracts": "workspace:*",
    "@ash/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests"
  }
}
